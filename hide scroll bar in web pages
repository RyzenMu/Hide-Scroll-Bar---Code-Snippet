/* For Chrome, Edge, and Safari */
::-webkit-scrollbar {
  display: none;
}

/* For Firefox */
* {
  scrollbar-width: none; /* Disables scrollbar */
}

/* Ensures content is still scrollable with mouse/keyboard */
.scrollable-container {
  overflow: hidden;
}
